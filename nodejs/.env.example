# Database
DATABASE_URL=postgresql://localhost:5432/assistant

# Server
PORT=3000
NODE_ENV=development

# Security
# Generate a secure API key for production: openssl rand -hex 32
# Leave empty or comment out to disable authentication (development only)
API_KEY=

# Logging
# Options: debug, info, warn, error
LOG_LEVEL=debug
